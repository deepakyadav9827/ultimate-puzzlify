import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deep.ultimatepuzzlify',
  appName: 'Ultimate Puzzlify',
  webDir: 'out'
};

export default config;